{
	"name": "@dsbunny/robust-task-schema",
	"version": "1.0.0",
	"description": "Robust task manager schema for JavaScript/TypeScript",
	"type": "module",
	"main": "dist/robust-task.schema.js",
	"types": "dist/robust-task.schema.d.ts",
	"author": "Steve-o <fnjordy@gmail.com>",
	"license": "MIT",
	"private": false,
	"scripts": {
		"build": "tsc",
		"clean": "rm -rf dist node_modules package-lock.json",
		"pull:upstream": "./scripts/pull-upstream.sh",
		"rebuild": "npm run clean && npm run pull:upstream && npm install && npm run build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"typescript": "^5.9.3",
		"zod": "^4.3.6"
	},
	"peerDependencies": {
		"zod": "^4.1.12"
	}
}
